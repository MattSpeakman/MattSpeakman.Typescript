var MattSpeakman;
(function (MattSpeakman) {
    // This is the shared scope service. It will hold all the page data.
    // It is NOT responsible for handling logic or Validation
    var SharedScopeService = (function () {
        function SharedScopeService() {
        }
        return SharedScopeService;
    }());
    MattSpeakman.SharedScopeService = SharedScopeService;
})(MattSpeakman || (MattSpeakman = {}));
